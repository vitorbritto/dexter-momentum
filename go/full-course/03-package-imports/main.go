package main

// imports brings external packages into the file that ur working
// where actually u need it

import (
	"fmt"
	"math"
)

func main() {

  // package.Function
  var msg string = "Package FMT imported running Prinln here."
  fmt.Println(msg)
  
  var sqrt float64 = math.Sqrt(5)
  fmt.Println(sqrt)
}